[package]
name = "pixus-macro"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "HTML-like html! procedural macro for Dioxus"

[lib]
proc-macro = true

[dependencies]
pixus-core = { version = "0.1.0", path = "../pixus-core" }
